version: "3.9"

services:
  db:
    image: pgvector/pgvector:pg17-trixie
    container_name: final112
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 9515
      POSTGRES_DB: final112
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
